# Required - app will not start without this
BEARER_TOKEN=

# Server settings
PORT=8000
HOST=0.0.0.0
COMMAND_TIMEOUT=30

# CORS - comma-separated list of allowed origins
ALLOWED_ORIGINS=https://jasonluther.github.io

# Container settings
CONTAINER_NAME=webify-bash
LOCAL_IMAGE_NAME=webify-bash
GHCR_IMAGE=ghcr.io/jasonluther/webify-bash:latest

# Commands config file path (relative or absolute)
COMMANDS_FILE=commands.json
